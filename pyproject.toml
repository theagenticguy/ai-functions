[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "strands-ai-functions"
dynamic = ["version"]
description = "AI Function decorator for enhancing functions with AI capabilities"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "Apache-2.0"}
authors = [
    {name = "AWS", email = "opensource@amazon.com"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "strands-agents>=1.24.0",
    "pydantic>=2.4.0,<3.0.0",
    "tstr>=0.4.0.post1",
    # The CLI (`ai-functions`) is built on the network layer and the
    # Typer/Rich/Textual stack, so these are runtime requirements.
    "websockets>=12.0",
    "cloudpickle>=3.0.0",
    "typer>=0.18.0",
    "rich>=13.7",
    "textual>=0.70",
    "platformdirs>=4.0",
    # Memory/optimizer rendering serializes parameter values to YAML.
    "pyyaml>=6.0.1",
    "smolagents>=1.24.0",
    "rank-bm25>=0.2.2",
]

[project.optional-dependencies]
network = [
    "websockets>=12.0",
    "cloudpickle>=3.0.0",
]
claude-code = [
    # 0.2.137 first exports ``ConversationResetMessage``, which the thread maps
    # to a CustomEvent; earlier releases cannot satisfy the import.
    "claude-agent-sdk>=0.2.137",
]
kiro = [
    "agent-client-protocol>=0.12.1,<0.13",
]
agentcore = [
    "bedrock-agentcore>=1.6.0",
]
runtime-tools = [
    "mcp>=1.29,<3",
    "uvicorn>=0.30",
]
codex = [
    "openai-codex>=0.147.0",
    # The Codex thread serves the runtime tools to the app-server over HTTP
    # MCP (CoordinatorToolServer), so the runtime-tools deps come along.
    "mcp>=1.29,<3",
    "uvicorn>=0.30",
]

[project.scripts]
ai-functions = "ai_functions.cli:main"

[project.urls]
Homepage = "https://github.com/strands-labs/ai-functions"
Repository = "https://github.com/strands-labs/ai-functions.git"
Issues = "https://github.com/strands-labs/ai-functions/issues"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_functions"]

[tool.hatch.envs.default]
python = "3.12"
dependencies = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0",
    "hypothesis>=6.92.0",
    "ruff>=0.1.0",
    "mypy>=1.20.0",
    "websockets>=12.0",
    "cloudpickle>=3.0.0",
    "claude-agent-sdk>=0.2.137",
    "typer>=0.18.0",
    "rich>=13.7",
    "textual>=0.70",
    "platformdirs>=4.0",
    "agent-client-protocol>=0.12.1,<0.13",
    "mcp>=1.29,<3",
    "uvicorn>=0.30",
    "openai-codex>=0.147.0",
    "pyyaml>=6.0.1",
    "bedrock-agentcore>=1.6.0",
    "rank-bm25>=0.2.2",
    "smolagents>=1.24.0",
    "strands-agents-tools>=0.8.2",
    "scipy>=1.11",
    "yfinance>=0.2.40",
    "plotly>=5.20",
]

[tool.hatch.envs.default.scripts]
test = "pytest {args:tests}"
test-cov = "pytest --cov=ai_functions {args:tests}"
lint = "ruff check src tests"
format = "ruff format src tests"
check-spec = "MYPYPATH=spec stubtest ai_functions --mypy-config-file pyproject.toml --allowlist stubtest-allowlist.txt"

[tool.pytest.ini_options]
testpaths = ["tests"]
# Examples import sibling helpers (e.g. ``_utils``); tests that load an example
# by file path need ``examples/`` importable as a top-level location.
pythonpath = ["examples"]
python_files = ["test_*.py", "*_test.py"]
asyncio_mode = "auto"
markers = [
    "integration: runs a real agent runtime binary (hermetic; model layer mocked)",
]
filterwarnings = [
    "ignore:websockets\\.legacy is deprecated:DeprecationWarning:websockets.legacy",
]

[tool.ruff]
line-length = 120
include = ["examples/**/*.py", "src/**/*.py", "tests/**/*.py"]

[tool.ruff.lint]
select = ["B", "D", "E", "F", "I", "UP", "W"]
ignore = ["D107"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D"]
"examples/**/*.py" = ["D"]
# Verbatim DS-1000 problem data: long embedded prompt/test strings can't wrap.
"examples/_ds1000_scipy.py" = ["D", "E501"]
"src/ai_functions/network/client.py" = ["D101", "D102", "D105"]
"src/ai_functions/network/endpoint.py" = ["D101", "D102", "D105"]
"src/ai_functions/network/channel.py" = ["D101", "D102", "D105"]
"src/ai_functions/network/wire_methods.py" = ["D101"]
# The gradient-feedback models and rendering helpers are below the public
# surface (the spec documents build_graph + TextGradOptimizer only).
"src/ai_functions/optimizer/textgrad.py" = ["D101"]
"src/ai_functions/optimizer/rendering.py" = ["D103"]

[tool.ruff.format]
docstring-code-format = true

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.12"
ignore_errors = true

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true

[tool.uv.workspace]
members = ["examples", "examples/skillsbench"]
